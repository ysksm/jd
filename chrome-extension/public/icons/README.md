# Extension Icons

To generate the extension icons:

1. Open `generate-icons.html` in a browser
2. Right-click each canvas and save as:
   - `icon16.png` (16x16)
   - `icon48.png` (48x48)
   - `icon128.png` (128x128)

Or use any icon generator to create blue icons with a "J" letter.
